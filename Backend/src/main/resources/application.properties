spring.application.name=Learnova

# DataSource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/learnova?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root

spring.jpa.open-in-view=false

# Hibernate properties
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Uncomment below to enable basic security (only for development)
# spring.security.enabled=true
# spring.security.user.name=soban
# spring.security.user.password=soban123
